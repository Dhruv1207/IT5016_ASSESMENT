def membership_total():
    d,n,m = gym_member()
    print("hello",n)
    add_on = int (input("how many add ons"))
    add_item={}

    for i in range (add_on):
        add_name = input("enter the add on")
        amount = float(input("how much"))

        add_item[add.name]=amount_total=0
    for k,v in add_item.items():
        total = total+v

    return total